<h1>Главная страница</h1>
<% @subpage.each do |page| %>
    <h3><%= page.title %></h3>
    <div><%= page.body %></div>
    <div><%= link_to "Читать далее...", subpage_path(page.sublink) %></div> 
<% end %>